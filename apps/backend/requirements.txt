# ============================================================================
# Backend dependencies — CareerVerse AI
# ============================================================================
# Cài đặt: pip install -r requirements.txt
# Python: 3.11+
#
# Sau khi cài Playwright, chạy thêm: playwright install chromium
# (Playwright dùng cho job board scraping ở module trends/jobs).
# ============================================================================

# ── Web framework ──────────────────────────────────────────────────────────
fastapi>=0.123.4
uvicorn[standard]>=0.38.0
starlette>=0.27.0
python-multipart==0.0.22
websockets==11.0.3

# ── Validation & settings ──────────────────────────────────────────────────
pydantic[email]>=2.12.5
pydantic-settings==2.1.0
python-dotenv==1.2.2

# ── Database ───────────────────────────────────────────────────────────────
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
psycopg[binary]>=3.1
asyncpg

# ── HTTP clients ───────────────────────────────────────────────────────────
httpx>=0.24.1
requests
aiohttp                  # used by trends/data_service.py

# ── Auth & security ────────────────────────────────────────────────────────
PyJWT==2.12.0
passlib[bcrypt]==1.7.4
bcrypt                   # explicit (passlib's bcrypt extra is fragile across versions)

# ── Logging & error tracking ───────────────────────────────────────────────
loguru
sentry-sdk               # used by app/core/error_tracking.py
tenacity                 # retry decorators (services/onetsvc.py)

# ── External AI APIs ───────────────────────────────────────────────────────
google-generativeai>=0.8.0

# ── Caching ────────────────────────────────────────────────────────────────
redis==5.0.1

# ── Cloud storage ──────────────────────────────────────────────────────────
boto3==1.34.0            # Cloudflare R2 (S3-compatible)

# ── PDF / document parsing ─────────────────────────────────────────────────
PyMuPDF>=1.23.0          # imported as `fitz`
pdfplumber>=0.10.0
pypdf>=4.0.0             # PDF parser (replaces deprecated PyPDF2)
pdf2image                # used by skill_gap/cv_parser_v2.py
python-docx>=1.1.0
fpdf2>=2.7.0             # imported as `fpdf`

# ── OCR & image processing ─────────────────────────────────────────────────
Pillow                   # imported as `PIL`
opencv-python            # imported as `cv2`
pytesseract              # requires system Tesseract binary
python-magic             # MIME detection (cv_validator)
                         # On Windows: pip install python-magic-bin instead

# ── ML / NLP / Embeddings ──────────────────────────────────────────────────
numpy
pandas                   # used by trends/data_service.py
sentence-transformers>=2.7.0
neo4j>=5.0.0

# ── Web scraping ───────────────────────────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=4.9.0              # used as BeautifulSoup parser
playwright               # Headless browser; run `playwright install chromium`

# ── Search ─────────────────────────────────────────────────────────────────
elasticsearch            # search/es_client.py — optional, only if ES is enabled

# ── Email / DNS ────────────────────────────────────────────────────────────
dnspython                # imported as `dns` — used by email_verifier
python-dateutil          # imported as `dateutil`

# ── Voice Interview System ─────────────────────────────────────────────────
edge-tts==6.1.9
gTTS                     # fallback TTS (interview/fallback_tts_service.py)
pyttsx3                  # offline TTS fallback
openai-whisper>=20231117 # imported as `whisper`
faster-whisper>=1.0.0    # 4-5x faster STT via CTranslate2
imageio-ffmpeg           # ffmpeg wrapper for whisper preprocessing
pydub==0.25.1
aiofiles==23.2.1

# ── Binary serialization ───────────────────────────────────────────────────
orjson>=3.9.0
msgpack>=1.0.7

# ── Scheduler ──────────────────────────────────────────────────────────────
apscheduler>=3.10.0
