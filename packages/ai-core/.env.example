# Database
# === Postgres (pgvector) ===
# Dạng URI (khuyên dùng):
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/career_ai?sslmode=prefer&connect_timeout=10

# Dạng DSN key=value (tuỳ chọn – chỉ dùng nếu bạn muốn):
# DATABASE_DSN=host=localhost port=5433 dbname=career_ai user=postgres password=postgres connect_timeout=10 sslmode=prefer
# Lưu ý: Chỉ sử dụng một trong hai biến trên.

NEO4J_URL=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASS=neo4j_password
ELASTICSEARCH_URL=http://localhost:9200

# HuggingFace / model hub
HF_TOKEN=your_hf_token_here

# App
APP_ENV=dev
LOG_LEVEL=INFO
