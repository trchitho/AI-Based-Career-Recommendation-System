# Frontend env
# Option A: call backend directly
NEXT_PUBLIC_API_BASE=http://localhost:8000
# Option B: use Next.js rewrite proxy (recommended in dev)
# PROXY_API_BASE overrides where /api/* rewrites to
PROXY_API_BASE=http://localhost:8000

